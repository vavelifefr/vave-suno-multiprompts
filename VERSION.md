# VERSION.md — release marker for vave-suno-multiprompts

- Version: 1.1.0
- Date: 2026-09-13
- Fork: cloned from vave-suno-prompts on 2026-09-12 (its test folder was excluded).
- Canon (normative): SKILL.md (Pro composed entry) + SKILL-CORE.md (process) + RULES.md (policy) + SUNO-TAGS.md (vocabulary) + ADAPTERS.md (runtime bindings); LITE.md + LITE-TAGS.md (Lite frozen pair); DEFAULTS.md (Pro runtime state).
- Human reference: README.md is the entry point (this skill is English-only by design).
- Entry point (humans): README.md — mission, map, quick start, triggers.
- Runtime: tools/suno-fill.user.js v3.9.0 (fill: 4 text fields + the seven More Options controls — Vocal Gender/Duration/Max Mode toggles, Weirdness/Style Influence/Variety numeric sliders driven asynchronously with read-back (45ms per step, stall detection), Personalize by Off/On or aria-pressed toggle; Variety mapped from the live labels 0=Off/1=Normal/2=High/3=Extra/4=Max; forces Advanced; model-aware; free/mini notice + safe style cap; contenteditable (Lyrics/Lexical) filled asynchronously — one synthetic `beforeinput`, wait ~450ms, verify, else `paste`, else `insertHTML`, else `textContent`; no visual marking of fields; one-click fill JSON report with per-field write path, `expected`, `moreOptionsTarget`/`moreOptionsApplied`, `controls` and counters/fields sampled ~700ms after the write plus an immediate snapshot; read-only diagnostics incl. slider visibility, control rows and a More Options probe; NEVER clicks Create; button labeled Fill, left-centered). Previous good build saved at tools/backups/suno-fill.v3.8.0.user.js. Tools: tools/measure.ps1 (UTF-8 README metrics) + tools/validate-block.ps1 (hardened structural/contract check) + tools/smoke.ps1 (smoke harness) + tools/check-encoding.ps1 (encoding audit/fix).
- Variants: **Pro is the default** (full stack); Lite is proposed only for a narrow window or when the full canon cannot be loaded. Pro = SKILL.md (composed) + SKILL-CORE.md + RULES.md + SUNO-TAGS.md + ADAPTERS.md + Pro runtime (DEFAULTS/GUIDE/LINKS/tools); Lite = LITE.md + LITE-TAGS.md, v6-mini default. Select gate: SKILL.md §0 → ADAPTERS.md A0.
- Setup: on first launch in a window, a five-question setup batch is asked once (DEFAULTS.md §F) right after the variant handshake: lyrics language, communication language, model, detail tier, scope. The chosen model is recorded in INFO `model:` (`v6` / `v6-wild` / `v6-mini`) and passed to the fill userscript, which checks it against the UI model chip and treats `v6-mini` as free (non-commercial, Pro-only features untouched).
- Memory: STATE v2 (package fingerprint `sha256:` under a shell stand, `structural:` elsewhere; CHANGES/RENDER; verified exact restore against the matching parser package; v1 accepted as continuity-only hint) per SKILL-CORE C5.
- Sources: LINKS.md (registry, DD.MM.YYYY stamps; all `never` until the first-launch sync) + GUIDE.md (immutable manual, cap 50000 chars) + STYLE-NOTES.md (mutable registry, cap 30000). The embedded reference in SKILL.md §7 was built from sources on 2026-09-11; this is separate from the runtime registry sync and is not a claim that LINKS.md was visited.
- Tests: structural smoke via tools/smoke.ps1 (validator micro-blocks incl. cross-field semantic probes, encoding policy, README metrics).
- Sync: SUNO-TAGS.md v2026-09-12, DEFAULTS.md v2026-09-13, embedded reference 2026-09-11, LINKS.md pending first-launch sync.
- Package: portable skill bundle — `SKILL.md` is the universal entry; the folder can be uploaded to any skill-capable platform with no host-specific registration. Concept ideation from real references is part of the skill (SKILL-CORE C2), so no separate agent is needed.
- History: CHANGELOG.md (normative archive).
