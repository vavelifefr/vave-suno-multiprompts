# OS / editor
Thumbs.db
Desktop.ini
.DS_Store

# Scratch / temp
*.tmp
*.bak
*.png

# Build artifacts
*.zip

# Hand-made duplicate copies (e.g. Windows "— копия")
*копия*

# Personal prompt archive — never uploaded; only .gitkeep is tracked
Prompts/*
!Prompts/.gitkeep
